<h1>Click on the movie to check showtime</h1>

<div class="grid">
    {{#each movie}}
    <div class='movie-block'>
    <img src="{{image}}"/>
    <h2>{{title}}</h2>
    <a href="/movies/{{_id}}">See more</a>
    </div>
    {{/each}}
</div>